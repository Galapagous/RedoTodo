{
  "name": "w3backend",
  "version": "1.0.0",
  "description": "w3dev todo backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm install-client && npm run build",
    "start": "node index.js",
    "client": "cd client && npm start"
  },
  "author": "muhammed musa",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.5.1",
    "nodemon": "^2.0.19"
  }
}
